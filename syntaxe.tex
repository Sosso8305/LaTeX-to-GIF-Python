%document classe peut changer
\documentclass[tikz]{standalone}

%usepackage&uselibrary

\tikzset{every figure} = [parametres de tikzpicture]

\begin{document}
	
	\begin{tikzpicture} 

		\node (nom node) at(x,y) [options separées par virgules] {affichage} ;
		\node ...
		...
		%options qui nous interessent : fill, label
		%option utilisable? double, 
		
		\path (nom node A) edge[options separées par virgules] (nom node B);
		\path (nom node A) edge[opt...
		...
		%arc ou arrete :option OBLIGATOIRE: -> ou -
		%options qui nous interessent : "color", 
		%option utilisable? weight, 
		
	\end{tikzpicture}
	
\end{document}

